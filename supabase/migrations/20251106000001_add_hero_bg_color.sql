-- ================================================
-- 東勝会社 CMSウェブサイト - ヒーロー背景色フィールド追加
-- ================================================
-- このマイグレーションファイルは:
-- 1. company_infoテーブルにhero_bg_colorフィールドを追加
-- ヒーローセクションの背景色を管理画面から設定可能にする
-- デフォルトは紫系のグラデーション (#8b5cf6)
-- ================================================

-- hero_bg_color フィールドを追加
ALTER TABLE company_info 
ADD COLUMN IF NOT EXISTS hero_bg_color text DEFAULT '#8b5cf6';

COMMENT ON COLUMN company_info.hero_bg_color IS 'ヒーローセクションの背景ベースカラー（円形グラデーション用）';

-- ================================================
-- 完了メッセージ
-- ================================================

DO $$
BEGIN
  RAISE NOTICE '✅ ヒーロー背景色フィールドの追加が完了しました！';
  RAISE NOTICE '📋 hero_bg_color フィールドがcompany_infoテーブルに追加されました。';
  RAISE NOTICE '🎨 デフォルト値: #8b5cf6 (紫色)';
END $$;
