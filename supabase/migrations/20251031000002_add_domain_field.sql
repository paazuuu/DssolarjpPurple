-- ================================================
-- 東勝会社 CMSウェブサイト - ドメイン情報の追加
-- ================================================
-- このマイグレーションファイルは:
-- 1. company_infoテーブルにドメイン情報フィールドを追加
-- を含みます。
-- ================================================

-- ------------------------------------------------
-- 1. ドメインフィールドの追加
-- ------------------------------------------------

-- 会社ドメイン（公式ウェブサイトのドメイン名）
ALTER TABLE company_info
ADD COLUMN IF NOT EXISTS domain text DEFAULT 'dssolarjp.com';

-- コメントを追加して用途を明確化
COMMENT ON COLUMN company_info.domain IS '会社の公式ウェブサイトのドメイン名（例: dssolarjp.com）';

-- ------------------------------------------------
-- 2. 完了メッセージ
-- ------------------------------------------------

DO $$
BEGIN
  RAISE NOTICE '✅ ドメインフィールドの追加が完了しました！';
  RAISE NOTICE '🌐 デフォルトドメイン: dssolarjp.com';
  RAISE NOTICE '📝 管理画面から変更可能です';
END $$;
